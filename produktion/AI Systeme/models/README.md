# Models Directory

Place Ollama model files here for centralized access from the network share.

All worker nodes pull models from this shared location.

## Structure

```
models/
├── llama2/
├── mistral/
├── llava/
└── whisper/
```

See `../models.json` for the full model registry.
